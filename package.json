{
  "name": "college-erp",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.5.2",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*",
    "tooling/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "turbo": "^1.12.3",
    "typescript": "^5.3.3"
  }
}
