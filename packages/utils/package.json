{
  "name": "@college-erp/utils",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json --emitDeclarationOnly",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "date-fns": "^3.2.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
